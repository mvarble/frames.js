/**
 * math.js
 *
 * This creates a subset of the Math.js module for the package.
 */

import {
  identity,
  matrix,
  transpose,
  inv,
  multiply,
  pow,
  cos,
  sin,
  norm,
} from 'mathjs';

export default {
  identity,
  matrix,
  transpose,
  inv,
  multiply,
  pow,
  cos,
  sin,
  norm,
};
